import React, { FC } from "react";
import {Card} from "@bank/ui-library";

export const Accounts: FC = (): JSX.Element => {
    return (
        <div>
            Accounts


            <Card>
                test
            </Card>
        </div>
    );
}
