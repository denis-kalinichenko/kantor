{
  "name": "kantor",
  "version": "0.1",
  "description": "Currency Exchange Widget",
  "repository": "https://github.com/denis-kalinichenko/kantor.git",
  "author": "Denis Kalinichenko <denis@kalinichenko.me>",
  "license": "UNLICENSE",
  "private": true,
  "workspaces": [
    "packages/ui-library",
    "packages/*",
    "applications/*"
  ],
  "scripts": {
    "prestart": "yarn clean && yarn workspaces run build",
    "start": "cd applications/client && yarn start",
    "test": "yarn workspaces run test --ci",
    "build": "yarn clean && yarn workspaces run build",
    "clean": "rimraf ./packages/react-currency-exchange-widget/build ./packages/ui-library/build applications/client/build"
  },
  "devDependencies": {
    "rimraf": "^3.0.2"
  }
}
